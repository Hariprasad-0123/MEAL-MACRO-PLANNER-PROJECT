{
  "name": "meal-macro-planner-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install && npm --prefix backend install && npm --prefix frontend install",
    "start": "concurrently \"npm --prefix backend start\" \"npm --prefix frontend run dev\"",
    "dev": "npm run start"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
